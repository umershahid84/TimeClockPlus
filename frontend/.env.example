# Base URL of the TimeClockPlus API. In local dev, the Vite proxy forwards
# /api to the backend, so this can stay unset. In production builds, set
# this to the deployed API's public URL.
VITE_API_BASE_URL=
